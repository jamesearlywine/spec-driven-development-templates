# UI Application Events (Spec)

<!-- Component-level slice: events these components participate in (not a duplicate of root application-events.spec.md). -->

## Scope

- This spec describes **UI-level** events (user actions, client-side state changes) that map to the root application events and to backend APIs.

## Event list

| UI Event | Trigger | Maps to (root event / API) |
|----------|---------|----------------------------|
| <!-- e.g. FormSubmit --> | User clicks Submit | ItemCreated / POST /items |

## Components involved

- **page-1**: <!-- Events on this page -->
- **component-1**: <!-- Events from this component -->
